%h1
  New feed
- semantic_form_for(@feed, :url => admin_feeds_path ) do |f|
  = f.error_messages
  = f.input :feed_title
  = f.input :feed_uri, :as => :string
  = f.buttons

= link_to 'Back', feeds_path

